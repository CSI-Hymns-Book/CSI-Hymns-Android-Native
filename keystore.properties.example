# Copy to keystore.properties and fill in your Play Store upload key details.
# Same values as CSI-Hymns-Main/android/key.properties — do NOT commit keystore.properties.
#
# storeFile=/absolute/path/to/upload-keystore.jks
# storePassword=your-store-password
# keyAlias=upload
# keyPassword=your-key-password
